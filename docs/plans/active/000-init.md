# Active Plan: 000-init

## Goal
Initialize the Crush Harness Workflow structure.

## Steps
- [x] Create `/docs` directory structure.
- [x] Create `TEMPLATE.md` for specs.
- [x] Create `DECISIONS.md` for architecture.
- [ ] User begins using the workflow by defining the first spec.

## Notes
- TestSprite is disabled per user request. We will use standard test runners (e.g., `pytest`, `jest`, `vitest`) depending on the tech stack.
